import Logo from "@/components/global/logo";

const HeaderAuth = () => {
  return (
    <header className="p-4 h-14 flex items-center">
      <Logo />
    </header>
  );
};

export default HeaderAuth;
